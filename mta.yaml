ID: TRAIL
_schema-version: '2.1'
version: 0.0.1

modules:
 - name: Trail_DB
   type: hdb
   path: Trail_DB
   requires:
    - name: hdi_Trail_DB

resources:
 - name: hdi_Trail_DB
   properties:
      hdi-container-name: ${service-name}
   type: com.sap.xs.hdi-container
